server:
  port: ${SERVER_PORT:8080}

security:
  admin:
    username: ${ADMIN_USERNAME:admin}
    email: ${ADMIN_EMAIL}
    password: ${ADMIN_PASSWORD}
  jwt:
    issuer: ${JWT_ISSUER}
    access-token:
      secret: ${ACCESS_TOKEN_SECRET}
      expiration-time-milis: ${ACCESS_TOKEN_EXPIRATION_TIME_MILIS:60000}
    refresh-token:
      secret: ${REFRESH_TOKEN_SECRET}
      expiration-time-milis: ${REFRESH_TOKEN_EXPIRATION_TIME_MILIS:604800000}